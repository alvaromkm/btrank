% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bt_rank_all.R
\name{bt_rank_all}
\alias{bt_rank_all}
\title{Fit a Bradley-Terry ranking in a single call}
\usage{
bt_rank_all(
  data,
  score_col = "score",
  item_col = "item",
  period_col = "period",
  higher_is_better = TRUE,
  top_n = NULL,
  half_life = NULL,
  digits = 4
)
}
\arguments{
\item{data}{A data frame with one row per item per period. Must contain
at least the columns specified in \code{item_col}, \code{period_col}, and
\code{score_col}.}

\item{score_col}{Name of the column containing the numeric metric used to
determine winners within each period. Character string. Default: \code{"score"}.}

\item{item_col}{Name of the column identifying items. Character string.
Default: \code{"item"}.}

\item{period_col}{Name of the column identifying periods. Character string.
Default: \code{"period"}.}

\item{higher_is_better}{Logical. If \code{TRUE} (default), higher values of
\code{score_col} indicate a better outcome. Set to \code{FALSE} for metrics where
lower is better (e.g. time, errors).}

\item{top_n}{Integer. If provided, only the top \code{top_n} items by ability
are returned. If \code{NULL} (default), all items are returned.}

\item{half_life}{Positive number. If provided, exponential decay weights
are computed via \code{\link[=bt_weights]{bt_weights()}} and passed to \code{\link[=bt_win_matrix]{bt_win_matrix()}}. The
most recent period receives weight 1; earlier periods receive
progressively lower weights. If \code{NULL} (default), all periods are
weighted equally.}

\item{digits}{Integer. Number of decimal places for ability columns.
Default: \code{4}.}
}
\value{
A data frame as returned by \code{\link[=bt_rank]{bt_rank()}}, with columns \code{rank},
\code{item}, \code{ability}, and \code{ability_norm}.
}
\description{
Convenience wrapper that runs the full Bradley-Terry pipeline in one step:
builds the win count matrix, fits the model, and returns a ranked data
frame. Optionally applies temporal weighting via exponential decay.
}
\details{
This function is equivalent to calling \code{\link[=bt_win_matrix]{bt_win_matrix()}}, \code{\link[=bt_fit]{bt_fit()}}, and
\code{\link[=bt_rank]{bt_rank()}} in sequence. For more control over individual steps, use those
functions directly.
}
\examples{
data <- data.frame(
  item   = c("A", "B", "C", "A", "B", "C"),
  period = c(2022, 2022, 2022, 2023, 2023, 2023),
  score  = c(80, 65, 50, 75, 85, 60)
)

# Full pipeline in one call
bt_rank_all(data, score_col = "score")

# With temporal weighting
bt_rank_all(data, score_col = "score", half_life = 1)

# Top 2 items only
bt_rank_all(data, score_col = "score", top_n = 2)

}
\seealso{
\code{\link[=bt_win_matrix]{bt_win_matrix()}}, \code{\link[=bt_fit]{bt_fit()}}, \code{\link[=bt_rank]{bt_rank()}}, \code{\link[=bt_weights]{bt_weights()}}
for the individual pipeline steps.
}
