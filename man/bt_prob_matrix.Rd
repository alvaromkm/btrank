% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bt_prob_matrix.R
\name{bt_prob_matrix}
\alias{bt_prob_matrix}
\title{Compute a pairwise win probability matrix from a fitted Bradley-Terry model}
\usage{
bt_prob_matrix(fit, order_by = c("ability", "name"))
}
\arguments{
\item{fit}{A \code{btfit} object as returned by \code{\link[=bt_fit]{bt_fit()}}.}

\item{order_by}{Character string controlling row/column order of the
output matrix. One of \code{"ability"} (default, strongest item first) or
\code{"name"} (alphabetical order).}
}
\value{
A square numeric matrix of dimension n_items × n_items. Entry
\verb{[i, j]} is the probability that item i beats item j. Diagonal entries
are \code{NA}. Row and column names are item identifiers.
}
\description{
For every ordered pair of items (i, j), computes the probability that item
i beats item j according to the Bradley-Terry model:
}
\details{
\deqn{P(i \text{ beats } j) = \frac{e^{\lambda_i}}{e^{\lambda_i} + e^{\lambda_j}}}

where \eqn{\lambda_i} and \eqn{\lambda_j} are the estimated log-abilities
from the fitted model.
}
\examples{
data <- data.frame(
  item   = c("A", "B", "C", "A", "B", "C"),
  period = c(2022, 2022, 2022, 2023, 2023, 2023),
  score  = c(80, 65, 50, 75, 85, 60)
)
mat <- bt_win_matrix(data, score_col = "score")
fit <- bt_fit(mat)

# Probability matrix ordered by ability (strongest first)
bt_prob_matrix(fit)

# Probability that item "A" beats item "C"
pm <- bt_prob_matrix(fit)
pm["A", "C"]

}
\seealso{
\code{\link[=bt_fit]{bt_fit()}} to fit the model, \code{\link[=bt_rank]{bt_rank()}} to extract a ranked
data frame from the same fit.
}
